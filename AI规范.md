# AI 协作规范

## 1. 凭据与敏感信息

- 禁止把账号、密码、Token、API Key、邮箱授权码、数据库连接串明文写进代码、README、部署文档、测试快照或提交信息。
- 本地开发凭据只能放在未跟踪文件、环境变量或受保护的密钥管理服务中。
- 当前项目本地凭据文件为 `账密.txt`，该文件必须保持在 `.gitignore` 中，禁止提交。
- 示例配置只能使用占位符，例如 `<set-admin-password>`、`<mail-app-password>`。
- 初始化账号时只能从环境变量或本地未跟踪凭据文件读取密码；缺失时应失败并提示配置，不允许静默使用默认密码。
- 登录页、README、部署文档不得展示可直接登录的账号密码。
- 如果发现凭据已进入 Git 历史，必须先提示风险，再由用户确认是否重写历史和强推远程。

## 2. 修改前检查

- 开始删除、迁移、重命名文档或方案前，必须先读文件内容并判断是否符合当前架构。
- 不得仅凭文件名判断“旧方案”或“新方案”。
- 改图谱、登录、数据库、部署配置前，必须先用 `rg` 扫描相关调用点。
- 修改凭据相关逻辑后，必须复扫关键词：`password`、`secret`、`token`、`api_key`、`NEO4J_PASSWORD`、`账密`、已知默认密码片段。

## 3. 环境与命令

- 执行任何 Python 脚本、pytest、Notebook 前，必须先激活 Conda 环境 `familytree`。
- 如果环境名不明确，必须停止并询问，禁止在默认 Python 环境运行。
- 前端命令在 `front` 目录执行；后端命令在 `after` 目录执行。
- 不使用破坏性 Git 命令，除非用户明确要求并确认风险。

## 4. 代码质量

- 每个函数头部需要有简要功能说明。
- 复杂逻辑内部需要少量关键注释，但不要写无意义注释。
- 文件中函数较多时，应按逻辑分区。
- 不为不确定的数据结构写过度兼容代码；遇到无法从项目中确认的接口或字段，必须停下确认。
- 不把简单线性逻辑拆成无意义的小函数。

## 5. 图谱系统约束

- 人物节点默认只显示姓名，年份和详情放到侧栏。
- `FamilyUnit` 是布局语义，不应渲染成可见的中间人物节点。
- 正式家谱图连线应横平竖直，避免自动折线和奇怪拐点。
- 鼠标悬停只突出当前人物，不允许全图高亮。
- 选中态只高亮边框，不允许整块强高亮。
- 家族全景必须使用独立布局，不能复用本家主线布局冒充全景。

## 6. 提交前检查

- 提交前必须检查 `git status --short`，确认没有误提交本地凭据、数据库、日志、构建产物。
- 提交前必须运行与改动相关的测试。
- 提交前必须再次扫描敏感信息。
- README 面向仓库展示，只写公开信息，不写本地账号、密码、内部吐槽或临时说明。
