# 族谱查询系统 - 技术栈文档

## 当前项目技术栈

### 前端
- **框架**: Vue 3 (Composition API + `<script setup>`)
- **构建工具**: Vite
- **可视化**: 原生 SVG + 手写布局算法
- **状态管理**: Vue 3 响应式 (ref, computed)
- **HTTP 客户端**: Axios
- **样式**: Scoped CSS (组件内样式)

### 后端
- **框架**: FastAPI (Python)
- **ORM**: SQLAlchemy
- **数据库**: MySQL
- **认证**: 简单的用户名密码认证
- **文件存储**: 本地文件系统 (uploads/)

### 部署
- **容器化**: Docker + Docker Compose
- **反向代理**: Nginx

---

## Gemini 建议分析

### 1. 配偶和女性家庭处理

#### 方案 A：合体卡片（推荐）
- 丈夫和妻子画在同一个节点卡片里：`[ 丈夫头像 | 妻子头像 ]`
- 子女的线直接从"家庭卡片"下方引出
- 岳父母家：点击妻子头像时，弹出侧边栏展示娘家谱系

#### 方案 B：伴随节点
- 配偶节点紧贴直系亲属节点，用短双横线连接
- 配偶不参与树的层级计算
- 布局算法只计算有血缘关系的人，算好位置后再把配偶"挂"在旁边

**当前项目状态**: 使用方案 B 的变体，但布局算法不够完善，导致母系家庭加入时出现混乱。

### 2. 动态折叠（Focus & Context）
- 默认只显示直系：父母、亲兄弟、妻子儿女
- 堂兄弟等默认折叠（显示为 [+] 按钮）
- 点击展开分支，自动收起无关分支

**当前项目状态**: 未实现，一次性展示所有人。

### 3. 推荐的布局算法/库

| 算法/库 | 说明 | 适用场景 |
|---------|------|----------|
| Reingold-Tilford | 经典树形结构算法 | 标准树形布局 |
| d3.js / d3-hierarchy | D3 的 tree 布局 | Web 树形可视化 |
| Elkjs | Eclipse Layout Kernel | 复杂层级图，防止线条穿过节点 |
| AntV G6 | 阿里图可视化引擎 | 树图、缩放、自定义节点 |
| React Flow / Vue Flow | 节点编辑器库 | 灵活的连线和布局 |

**当前项目状态**: 手写布局算法，未使用成熟库。

### 4. 已实现的功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 缩放与拖拽 | ✅ 已实现 | 鼠标滚轮缩放，拖拽画布 |
| 搜索定位 | ✅ 已实现 | 模糊搜索 + 平滑动画定位 + 高亮 |
| 添加人员 | ✅ 已实现 | 侧边栏表单，支持父母/配偶/孩子关系 |
| 人物详情 | ✅ 已实现 | 侧边栏抽屉展示 |
| 关系查询 | ✅ 已实现 | LCA 算法 + 中文称呼计算 |
| 鹰眼/小地图 | ❌ 未实现 | - |
| LOD 层级显示 | ✅ 部分实现 | 缩小时显示圆点/名称，放大显示卡片 |
| 动态折叠 | ❌ 未实现 | - |
| 右键上下文菜单 | ❌ 未实现 | 目前通过侧边栏添加 |

### 5. 数据库选型

| 类型 | 优点 | 缺点 |
|------|------|------|
| **MySQL (当前)** | 熟悉、稳定、生态好 | 复杂关系查询麻烦 |
| Neo4j (图数据库) | 天生适合族谱、查询简单 | 学习成本、部署复杂 |
| PostgreSQL | 支持递归 CTE、JSON | 比 MySQL 稍复杂 |

**当前项目**: 使用 MySQL + 邻接表设计（father_id, mother_id）

---

## 优化建议优先级

### 高优先级
1. **配偶合体卡片** - 解决布局混乱问题
2. **动态折叠** - 解决大数据量显示问题

### 中优先级
3. **引入成熟布局库** - 如 d3-hierarchy 或 Elkjs
4. **鹰眼小地图** - 提升大图导航体验

### 低优先级
5. **右键上下文菜单** - 提升添加人员体验
6. **图数据库迁移** - 如果 MySQL 性能不足再考虑

---

## 文件结构

```
Family_tree/
├── front/                    # 前端 (Vue 3 + Vite)
│   ├── src/
│   │   ├── App.vue          # 主组件（包含大部分逻辑）
│   │   ├── api/             # API 接口
│   │   ├── components/      # 子组件
│   │   └── styles/          # 样式
│   └── vite.config.js
├── after/                    # 后端 (FastAPI)
│   ├── app/
│   │   ├── main.py          # 入口
│   │   ├── models.py        # 数据库模型
│   │   ├── schemas.py       # Pydantic 模型
│   │   ├── database.py      # 数据库连接
│   │   ├── routers/         # API 路由
│   │   └── services/        # 业务逻辑
│   ├── uploads/             # 上传文件
│   └── requirements.txt
└── deploy/                   # 部署配置
    ├── docker-compose.yml
    ├── Dockerfile.backend
    ├── Dockerfile.frontend
    └── nginx.conf
```
