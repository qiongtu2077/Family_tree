# Family_tree 重构规划

## 结论先行

我建议把“族谱关系数据”迁移到 Neo4j，把前端从“手写 SVG + 临时坐标布局”重构为“图数据模型 + 自动布局引擎 + 可交互画布”的结构。

但需要先澄清一个关键点：Neo4j 很适合存储、查询和分析关系图，但它本身不负责我们网页里的拖拽交互。Neo4j Browser / Aura Explore 可以可视化查询结果，但项目里的前端拖拽、缩放、节点编辑、无交叉布局，仍然要由前端图形库和布局算法完成。

推荐方向：

- 数据库：Neo4j 作为族谱核心数据库。
- 后端：FastAPI 保留，SQLAlchemy 逐步替换为 Neo4j 官方 Python Driver。
- 前端：Vue 3 保留，族谱画布重构为独立模块。
- 布局：优先使用 ELK.js / AntV G6 的分层布局能力，结合“家庭单元 FamilyUnit”数据模型降低交叉。
- 交互：节点拖拽只作为临时观察或编辑排序入口，不把任意坐标当成族谱关系事实。

## 当前项目判断

当前后端是 FastAPI + SQLAlchemy + MySQL，核心模型是 `Person` 表，使用 `father_id`、`mother_id` 自关联来表达父母关系。查询族谱时，后端会把数据库数据临时构造成 NetworkX 有向图，再返回 `nodes` 和 `edges` 给前端。

当前前端主要是 Vue + 原生 SVG。虽然项目里依赖了 `@antv/g6`，但主画布逻辑仍然集中在 `App.vue` 里手写：布局、节点绘制、连线、缩放、拖拽、弹性回位、搜索定位等都混在同一个大文件中。

这说明目前最大的瓶颈不是单一技术点，而是三件事耦合在一起：

- 数据模型把复杂亲缘关系压扁成父亲/母亲两个字段。
- 后端每次请求临时构图，图查询能力没有真正落到数据库层。
- 前端同时负责数据解释、布局计算、图形绘制和交互状态，后续很难维护。

## 数据库方案比较

### 1. MySQL / MariaDB

优点：

- 当前项目已经在用，迁移成本最低。
- Navicat 管理方便，备份、部署、权限管理成熟。
- 普通 CRUD、用户账号、审核记录、上传文件元数据很稳。

缺点：

- 父母、配偶、收养、再婚、同父异母、资料来源、关系置信度等关系会越做越别扭。
- 多跳亲缘查询需要递归 SQL 或在后端组图，代码复杂度会上升。
- 对“查询两个人的关系路径”“查某个分支全部亲缘关系”这类问题不天然。

适用场景：

- 数据规模小、只做父母子女关系、短期不大改 UI。

不推荐作为重构后的核心族谱数据库。

### 2. PostgreSQL

优点：

- 比 MySQL 更适合复杂查询，支持 `WITH RECURSIVE` 做层级/树形递归。
- 事务、约束、索引、JSONB、全文搜索、扩展生态都强。
- 如果以后要严肃做账号、权限、审计、统计，PostgreSQL 很合适。

缺点：

- 本质仍是关系型数据库。族谱一旦从“树”变成“图”，SQL 会变复杂。
- 亲缘路径、任意深度遍历、跨分支关系查询，不如图数据库直观。
- 前端可视化仍然需要单独的图模型转换层。

适用场景：

- 想保持传统数据库体系，同时增强查询能力。
- 后续需要复杂账号系统、日志审计、报表。

可作为配套数据库，但不建议替代 Neo4j 做族谱核心关系图。

### 3. Neo4j

优点：

- 族谱天然是图：人是节点，亲缘/婚姻/收养/资料来源是关系。
- Cypher 查询亲缘路径、多跳祖先/后代、两人最短关系路径更直观。
- 关系可以携带属性，例如关系类型、排序、置信度、来源、时间范围。
- 支持唯一约束和索引，能保证 `personId`、`username` 等关键字段唯一。
- 后端不再需要每次把全库搬到 NetworkX 里才能做基本图查询。

缺点：

- 需要学习 Cypher，现有 SQLAlchemy 模型要重写。
- 运维会比单 MySQL 多一些认知成本。
- 不是所有数据都必须进图；账号、审计、文件等非图数据放 Neo4j 虽然可行，但不是它最舒服的领域。
- Neo4j 不等于前端拖拽组件，网页交互仍要自己做。

适用场景：

- 族谱关系会继续复杂化。
- 需要关系路径、分支查询、图谱探索。
- 希望数据模型和真实世界的家庭关系更一致。

推荐作为本项目的核心关系数据库。

### 4. ArangoDB

优点：

- 多模型数据库，能同时表达文档、键值、图。
- 支持图遍历、最短路径、k 最短路径等能力。
- 一个数据库里同时存人物文档和关系边，模型弹性大。

缺点：

- 国内资料、团队熟悉度、可视化和周边生态通常不如 Neo4j 普及。
- 对这个项目来说，多模型优势未必能抵消学习和维护成本。
- 如果我们主要问题就是族谱图关系，Neo4j 更聚焦。

适用场景：

- 既要文档数据库灵活性，又要图遍历。
- 后续要做更多非族谱的内容型数据。

不是首选，但可以作为 Neo4j 的备选。

### 5. MongoDB

优点：

- 人物档案、照片、履历、扩展字段用文档表达很自然。
- `$graphLookup` 能做一定递归查询。
- 适合字段经常变化、以单个人物档案为中心的产品。

缺点：

- 图关系不是核心模型，复杂亲缘路径和图约束不如 Neo4j。
- 递归查询结果排序、内存限制、复杂路径语义都需要额外处理。
- 婚姻/再婚/收养/跨支关系会逐渐变成应用层负担。

不推荐作为族谱核心关系数据库。

### 6. SQLite

优点：

- 本地开发简单，备份就是一个文件。
- 适合 demo 或极小规模单机使用。

缺点：

- 不适合多人协作、服务端生产部署和复杂图查询。

只适合作为开发测试数据库，不适合作为目标架构。

### 7. RDF / Triple Store

优点：

- 适合做知识图谱、语义推理、标准本体。
- 如果未来要接入公共谱系标准或语义网，表达力强。

缺点：

- 对当前产品过重。
- 前后端开发成本明显高于收益。

暂不推荐。

## 最终推荐

最适合当前项目的是 Neo4j。

原因不是“Neo4j 能拖拽”，而是它能把族谱的关系事实存对。前端拖拽和二维表达要另外设计。

建议采用两个阶段：

第一阶段：Neo4j 单核心方案。

- 把 `Person`、亲子、婚姻、收养、家庭单元迁移到 Neo4j。
- 账号、审核、用户与人物绑定也可以先放 Neo4j，降低系统复杂度。
- 保持 FastAPI 接口形状尽量稳定，前端先不大面积改业务流程。

第二阶段：按复杂度决定是否拆分数据库。

- 如果账号、权限、审计、通知、文件资产变复杂，再引入 PostgreSQL。
- Neo4j 专注族谱图，PostgreSQL 专注账号和业务记录。

当前不建议一开始就 MySQL + PostgreSQL + Neo4j 三套并行，会把重构复杂度拉得太高。

## Neo4j 数据模型建议

不要只把当前的 `father_id`、`mother_id` 机械翻译成两条边。族谱图里真正重要的是“家庭单元”。

建议核心节点：

- `(:Person)`：一个人。
- `(:FamilyUnit)`：一次家庭/婚姻/伴侣单元，可以有一个或两个伴侣，也可以表示未知父母组合。
- `(:User)`：登录账号。
- `(:Media)`：头像、照片、文档等资源。
- `(:Source)`：资料来源，例如族谱书、口述记录、导入文件。

建议核心关系：

- `(:Person)-[:PARTNER_IN]->(:FamilyUnit)`：某人属于某个伴侣/婚姻单元。
- `(:FamilyUnit)-[:HAS_CHILD {birthOrder}]->(:Person)`：家庭单元生育或抚养了某个孩子。
- `(:Person)-[:PARENT_OF {kind, side, certainty, sourceId}]->(:Person)`：可选的直接亲子边，便于路径查询。
- `(:Person)-[:SPOUSE_OF {startDate, endDate, order}]-(:Person)`：可选冗余边，用于快速查配偶。
- `(:User)-[:BOUND_TO]->(:Person)`：账号绑定人物。
- `(:Person)-[:HAS_MEDIA]->(:Media)`：人物媒体资源。
- `(:Source)-[:SUPPORTS]->(relationship)`：资料来源支持某条关系，Neo4j 对“关系指向关系”不直接建模时，可用中间证据节点表达。

为什么引入 `FamilyUnit`：

- 夫妻和孩子不是简单的两条线，而是一个家庭组合到多个孩子的超边。
- 再婚、多任配偶、单亲、未知父母、收养都更容易表达。
- 前端布局可以把 `FamilyUnit` 当成一个很小的虚拟节点，用它统一汇聚连线，明显减少交叉和重复线。

## 二维族谱表达的核心问题

族谱不是严格的树，而是图。

如果只看“某个人的父母、祖先、后代”，它局部像树；但一旦加入婚姻、再婚、堂表亲婚配、收养、跨分支关系、同父异母、同母异父，它就会变成有向无环图，甚至在某些现实关系中出现复杂交叉结构。

因此，一个完整族谱图不一定存在“所有节点、所有关系、二维、无交叉、无损”的布局。这个限制不是前端库的问题，而是图论层面的限制：任意图不保证是平面图；即使图是平面的，固定代际方向、固定配偶相邻、固定出生顺序这些业务约束也可能让无交叉布局不可行。

所以产品上应该坚持：

- 数据无损：数据库保存完整关系。
- 视图可控：每次只展示一个目标明确的投影。
- 主视图无交叉：主视图不强行展示所有关系。
- 辅助视图补全：被隐藏的跨支关系用路径、高亮、徽标、侧栏补充。

## 推荐的族谱显示方式

### 1. 主视图：中心人物谱系图

这是默认视图。

特点：

- 以选中的人为中心。
- 上方显示祖先，下方显示后代，同代显示配偶和兄弟姐妹。
- 使用 `FamilyUnit` 虚拟节点汇聚夫妻到子女的关系。
- 只展示选中范围内的“主谱系边”，保证画面尽量无交叉。
- 跨支关系不直接拉长线，而是在节点上显示关系徽标，点击后进入路径视图。

这个视图适合日常浏览、搜索定位、查看人物详情。

### 2. 关系路径视图

用于回答“两个人是什么关系”。

特点：

- 输入人物 A 和人物 B。
- 后端用 Neo4j 查询最短亲缘路径或规则化亲缘路径。
- 前端只画这条路径和必要旁支，天然可做到无交叉。
- 侧栏给出中文关系解释，例如“某某是某某的堂兄”“某某是某某的外曾祖父”。

这个视图比在全图里强行找线更清楚。

### 3. 分支视图

用于看某一支后代。

特点：

- 以某个祖先或某个 `FamilyUnit` 为根。
- 只往下展开后代。
- 适合导出、打印、分享。
- 可以严格按出生顺序、婚姻顺序排列。

这个视图最接近传统族谱。

### 4. 全图探索视图

用于管理和排查数据。

特点：

- 可以使用力导向、径向、网络布局。
- 允许少量交叉，因为目标是发现结构，而不是作为正式族谱图。
- 适合管理员找孤立节点、重复人物、异常关系。

不要把全图探索视图作为用户默认入口。

### 5. 时间轴视图

用于讲故事。

特点：

- 横轴是时间，纵向是家族分支或代际。
- 展示出生、婚姻、迁居、死亡、成就等事件。
- 解决“树图表达不了人生事件”的问题。

这是后续增强项，不是第一期必须。

## 前端拖拽方案

拖拽要分成三类，不能混在一起。

### 1. 观察拖拽

用户临时拖动节点，看清被遮挡的连线。

- 不写入数据库。
- 松手可以回弹，或提供“恢复布局”。
- 当前项目已有类似能力，可以保留理念，但实现需要模块化。

### 2. 排序拖拽

用户调整同辈顺序、配偶顺序、分支左右顺序。

- 写入的是 `birthOrder`、`displayOrder`、`branchOrder` 这类布局提示。
- 不直接保存绝对坐标。
- 保存后重新跑布局。

这是最适合族谱的持久化拖拽。

### 3. 建关系拖拽

管理员从一个人物的端口拖到另一个人物，创建父子、配偶、收养等关系。

- 拖拽完成后必须弹出确认表单。
- 用户明确选择关系类型。
- 后端校验是否会造成不合理环路、重复关系、性别/时间冲突。

不要仅凭“拖到某个位置”推断亲缘关系。

## 布局技术选择

### 方案 A：ELK.js + Vue/SVG 或 Canvas

优点：

- ELK 是专门做自动图布局的引擎，适合分层图、DAG、复杂连线。
- 可以把 Neo4j 返回的图转换成 ELK graph，拿到稳定坐标后自己渲染。
- 适合我们做“主谱系无交叉优先”的定制策略。

缺点：

- 渲染、事件、选择框、拖拽还要自己做，或者结合其他图形库。

适合追求布局质量和可控性。

### 方案 B：AntV G6

优点：

- 当前项目已经安装过 G6，迁移认知成本较低。
- 内置 compact-box、dendrogram、dagre、force、radial 等布局。
- 交互、节点、边、缩放、拖拽、状态管理比手写 SVG 完整。

缺点：

- 对家谱这种“夫妻单元 + 多子女 + 多婚姻”的特殊布局，仍需要大量定制。
- 需要评估当前依赖版本，可能要升级。

适合快速落地第一版交互图。

### 方案 C：Cytoscape.js

优点：

- 交互图能力成熟，支持拖拽、缩放、选择、事件、布局扩展。
- 适合全图探索和管理员数据排查。

缺点：

- 默认不是为族谱/家谱排版设计，正式谱系图需要额外布局算法。

适合作为“全图探索视图”，不一定适合作为主谱系图。

### 方案 D：D3

优点：

- `d3.tree` 的 tidy tree 适合纯树。
- 自由度最高，可以做非常细腻的 SVG 表达。

缺点：

- D3 是底层工具，不是完整图编辑器。
- 当前项目已经证明手写太多会导致复杂度堆到一个文件里。

适合局部图形和动效，不建议作为全部交互框架的唯一基础。

## 推荐前端架构

建议拆成四层：

1. 数据层
   - 负责调用 API。
   - 把后端返回的 Neo4j 风格数据标准化为前端 `GraphDTO`。

2. 布局层
   - 输入 `GraphDTO`。
   - 输出节点坐标、边路由、层级、可见性。
   - 可以先用 G6/dagre，后续替换 ELK.js。

3. 渲染层
   - 只负责画节点、线、头像、徽标、选中态。
   - 不直接懂业务查询。

4. 交互层
   - 管理搜索、定位、缩放、拖拽、节点点击、关系创建。
   - 把“用户动作”转换成业务命令，再交给 API。

第一期可以先拆出：

- `FamilyGraphCanvas.vue`
- `PersonNode.vue`
- `GraphToolbar.vue`
- `PersonDetailDrawer.vue`
- `useFamilyGraphLayout.js`
- `useFamilyGraphInteractions.js`

## 后端 API 规划

建议新 API 不再只返回普通 `nodes` / `edges`，而是返回适合布局的图数据。

核心接口：

- `GET /api/graph/focus/{personId}?generations=5&mode=centered`
  - 返回中心人物谱系图。

- `GET /api/graph/branch/{familyUnitId}?depth=5`
  - 返回某个家庭单元或祖先分支。

- `GET /api/graph/relation-path?from={id}&to={id}`
  - 返回两个人的关系路径和中文关系解释。

- `POST /api/persons`
  - 创建人物。

- `POST /api/family-units`
  - 创建家庭单元。

- `POST /api/relationships`
  - 创建亲子、配偶、收养等关系。

- `PATCH /api/layout-hints`
  - 保存出生顺序、展示顺序、分支顺序等布局提示。

## 迁移步骤

### 第 0 阶段：冻结语义

先确认这些规则，避免重构时猜错：

- 父母是否只允许一父一母，还是允许养父母/继父母？
- 配偶是否允许多任？
- 是否需要记录婚姻起止日期？
- 是否需要记录关系来源和可信度？
- 同名人物如何去重？
- 已故、失联、未知人物如何表示？
- 是否要支持 GEDCOM 导入/导出？

这些问题必须先定，不然 Neo4j 模型会反复返工。

### 第 1 阶段：建立 Neo4j 原型

- Docker Compose 增加 Neo4j。
- 建立约束：`Person.personId`、`User.username`、`FamilyUnit.familyUnitId` 唯一。
- 写少量 Cypher 初始化脚本。
- 用当前 MySQL 数据导出一份 JSON/CSV 测试导入。

### 第 2 阶段：后端数据访问层重构

- 引入 Neo4j 官方 Python Driver。
- 新建 repository/service 层，隔离 Cypher。
- 保持现有 API 尽量兼容，先让前端能跑。
- 再逐步新增 `focus graph`、`relation path`、`layout hints` 接口。

### 第 3 阶段：前端画布重构

- 从 `App.vue` 抽出画布模块。
- 先实现中心人物谱系图。
- 加入布局引擎，减少手写坐标计算。
- 再恢复搜索定位、详情面板、管理员编辑。

### 第 4 阶段：关系查询和编辑增强

- 两人关系路径。
- 从图上创建关系。
- 关系校验。
- 异常数据提示，例如重复父母、循环祖先、孤立节点。

### 第 5 阶段：导出和分享

- 分支图导出 PNG/SVG/PDF。
- 人物详情导出。
- 只读分享链接。

## 风险与边界

最大风险不是 Neo4j，而是“想在一个二维画面里无损展示整个复杂家族图且完全无交叉”。

建议把这件事拆开：

- 数据库负责完整、准确、可查询。
- 主谱系图负责清晰、稳定、少交叉。
- 关系路径图负责解释两个人关系。
- 全图探索图负责管理员排查。

这样产品会更可靠，也更符合用户直觉。

## 下一步建议

下一步不要直接写代码，先做一个很小的技术验证：

1. 选 20 到 50 个真实或模拟人物。
2. 覆盖父母、兄弟姐妹、夫妻、再婚、单亲、收养、跨分支关系。
3. 用 Neo4j 建模。
4. 后端返回 `Person + FamilyUnit + edges`。
5. 前端用 G6 或 ELK.js 渲染中心人物谱系图。
6. 专门验证两件事：关系查询是否舒服，主视图是否能控制交叉。

验证通过后，再正式迁移当前项目。

## 参考资料

- Neo4j Cypher core concepts: https://neo4j.com/docs/cypher-manual/25/queries/concepts/
- Neo4j Python Driver: https://neo4j.com/docs/python-manual/current/
- Neo4j constraints and indexes: https://neo4j.com/docs/cypher-manual/current/schema/constraints/create-constraints/
- Neo4j Graph Data Science path finding: https://neo4j.com/docs/graph-data-science/current/algorithms/pathfinding/
- PostgreSQL recursive queries: https://www.postgresql.org/docs/current/queries-with.html
- ArangoDB graphs: https://docs.arango.ai/arangodb/4.0/graphs/
- MongoDB `$graphLookup`: https://www.mongodb.com/docs/manual/reference/operator/aggregation/graphlookup/
- AntV G6 layouts: https://g6.antv.antgroup.com/en/manual/layout/base-layout
- ELK documentation: https://eclipse.dev/elk/documentation.html
- D3 tree layout: https://d3js.org/d3-hierarchy/tree
- Cytoscape.js documentation: https://js.cytoscape.org/
